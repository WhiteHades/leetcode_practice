# Optional env-only LeetCode auth for the Night Slayer TypeScript CLI.
# Normal path: run `make login` and paste cookies into the CLI.
# Env path: copy this file to .env and fill both values below.
LEETCODE_SESSION=
LEETCODE_CSRF_TOKEN=
