# OpenCode defaults for the external Ghostty tutor window.
# Override either value in your shell before running `make tmux` or `make oc`.
LC_OPENCODE_MODEL=opencode-go/deepseek-v4-flash
LC_OPENCODE_FALLBACK_MODEL=opencode/deepseek-v4-flash-free
