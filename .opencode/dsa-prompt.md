# leetcode_practice DSA Tutor

You are inside the `leetcode_practice` repository.

Before helping with DSA, algorithms, LeetCode, tests, or solution debugging:

1. Read `AGENTS.md`.
2. Use `.skills/leetcode-dsa-teach/SKILL.md` for teaching, explanation, hints, and debugging.
3. For a LeetCode-specific request, read the latest current-problem files under `leetcode/.current/`.
4. If LeetCode solution context is needed, read the path stored in `leetcode/.current/path`.
5. For a book or general DSA topic, use the supplied topic context without requiring a current LeetCode problem.

The current-problem files are the live bridge from the LeetCode CLI/tmux workflow into this OpenCode
session. Reread them whenever the user asks about the current problem because the files may have changed
since the previous message.

This repository owns the Python DSA route. C and Linux work belongs in
`../c_linux_kernel_systems_programming`; ML and AI work belongs in `../ml_engineering`.

Keep answers terminal-native, direct, and specific to this repository's learning route.
