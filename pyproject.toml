[project]
name = "leetcode_practice"
version = "0.1.0"
description = "Terminal first LeetCode practice with tmux, Neovim, and an OpenCode tutor."
readme = "README.md"
requires-python = ">=3.12,<3.14"
dependencies = [
  "ipython>=9.0.0",
]
